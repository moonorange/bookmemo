<h1 class="current_category"><%= params[:category] %></h1>

<% if @books.count == 0 %>
  <h1>このカテゴリーの本はまだありません</h1>
  
<% else %>

  <% @books.each do |book| %>
    <h2><%= link_to book.title, book_path(book) %></h2>
    <% end %>

<% end %>
